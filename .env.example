# Backend origin the landing page calls for POST /api/subscribe.
# Leave empty in local dev to use the Vite proxy (see vite.config.ts).
# In production set it to the atlasz backend origin, e.g. https://api.atlasz.app
VITE_API_BASE_URL=
